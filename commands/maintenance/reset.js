module.exports = {
    name: 'reset',
    description: 'Executa todos os procedimentos quando o bot é adcionado ao servidor.',
    guildOnly: true,
    permissions: 'ADMINISTRATOR',
    execute(message) {
        message.channel.send(`falta implementar`);
    },
};